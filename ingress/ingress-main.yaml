apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-nginx
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /api/ml
            backend:
              serviceName: krakend-api-gateway
              servicePort: 8080
          - path: /
            backend:
              serviceName: react-client
              servicePort: 3000